{
  "name": "telepilot",
  "version": "0.1.0",
  "private": true,
  "description": "Telepilot orchestration and autocomplete runtime",
  "scripts": {
    "test": "node --test",
    "lint": "npm run test"
  }
}
